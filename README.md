Generates https://nixcats.org via neovim's TOhtml for in-editor help files and pandoc for readme and table of contents.
```bash
nix run --refresh --no-write-lock-file github:BirdeeHub/nixCats-doc # -- "$OUTPATH"
```
